data:
  max_length: 50
  vocab_size: 22

model:
  hidden_dim: 256
  latent_dim: 128
  num_heads: 8
  num_layers: 4
  dropout: 0.1

train:
  batch_size: 64
  num_epochs: 100
  learning_rate: 1e-4
  weight_decay: 1e-5
  grad_clip_norm: 1.0
  device: cuda
  log_every: 5
  patience: 15